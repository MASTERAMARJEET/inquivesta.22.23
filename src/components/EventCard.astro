---
export interface Props {
  name: string
  url: string
}
const { name, url } = Astro.props
---

<a href={url}>
  <div class="p-12 rounded-lg bg-gray-700 lg:w-[45vw] w-[60vw] m-auto">
    <p class="font-bold text-center text-2xl py-2">{name}</p>
  </div>
</a>
